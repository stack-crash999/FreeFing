<UserControl x:Class="NetSentry.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="#0E131F">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="32,28,32,28">
        <StackPanel MaxWidth="1200" HorizontalAlignment="Stretch">

            <!-- ── Header ── -->
            <Grid Margin="0,0,0,24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="22" Foreground="#2563EB" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="System &amp; Network Settings" FontSize="24" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Configure adapter bindings, discovery engine intervals, and ARP firewall parameters"
                               FontSize="13" Foreground="#94A3B8" Margin="32,4,0,0" />
                </StackPanel>
            </Grid>

            <!-- ── 2 Columns of Settings ── -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Column: Adapter & Discovery -->
                <StackPanel Grid.Column="0">
                    <!-- Adapter Card -->
                    <Border Background="#182030" BorderBrush="#232E45" BorderThickness="1" CornerRadius="12" Padding="20,18" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="ACTIVE NETWORK ADAPTER" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Adapter Name" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AdapterNameBlock" Text="Primary Wi-Fi / Ethernet" Foreground="White" FontWeight="SemiBold" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Interface IPv4" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AdapterIpBlock" Text="192.168.1.50" Foreground="#38BDF8" FontFamily="Consolas" FontWeight="SemiBold" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Active Subnet CIDR" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AdapterSubnetBlock" Text="192.168.1.0/24" Foreground="White" FontFamily="Consolas" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Default Gateway" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AdapterGatewayBlock" Text="192.168.1.1" Foreground="#10B981" FontFamily="Consolas" FontWeight="SemiBold" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Discovery Preferences -->
                    <Border Background="#182030" BorderBrush="#232E45" BorderThickness="1" CornerRadius="12" Padding="20,18">
                        <StackPanel>
                            <TextBlock Text="SCAN &amp; TELEMETRY ENGINE" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="Background Polling Cycle" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <ComboBox SelectedIndex="0" Width="140" HorizontalAlignment="Right">
                                    <ComboBoxItem Content="Every 6 Seconds" />
                                    <ComboBoxItem Content="Every 15 Seconds" />
                                    <ComboBoxItem Content="Every 30 Seconds" />
                                </ComboBox>
                            </Grid>

                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="Multicast DNS (mDNS) Discovery" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <CheckBox IsChecked="True" Content="Enabled" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="UPnP / SSDP Device Probing" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <CheckBox IsChecked="True" Content="Enabled" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid>
                                <TextBlock Text="Strict Subnet Isolation" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <CheckBox IsChecked="True" Content="Enabled" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Right Column: Diagnostics & About -->
                <StackPanel Grid.Column="2">
                    <!-- Maintenance & Database -->
                    <Border Background="#182030" BorderBrush="#232E45" BorderThickness="1" CornerRadius="12" Padding="20,18" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="CACHE &amp; DATABASE DIAGNOSTICS" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="0,0,16"/>

                            <TextBlock Text="NetSentry caches identified vendor OUIs and hardware models in a fast local SQLite store for instant offline lookups."
                                       FontSize="12" Foreground="#94A3B8" TextWrapping="Wrap" Margin="0,0,0,16" LineHeight="17"/>

                            <StackPanel Orientation="Horizontal">
                                <Button Content="Clear Cache Database" Click="ClearDb_Click"
                                        Style="{StaticResource TopNavButtonStyle}" Padding="14,8" Margin="0,0,10,0"/>
                                <Button Content="Test Network Bridge" Click="TestBridge_Click"
                                        Style="{StaticResource TopNavButtonStyle}" Padding="14,8"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Application Identity -->
                    <Border Background="#182030" BorderBrush="#232E45" BorderThickness="1" CornerRadius="12" Padding="20,18">
                        <StackPanel>
                            <TextBlock Text="APPLICATION INFORMATION" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="0,0,16"/>

                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Product" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Text="FreeFing / NetSentry Desktop" Foreground="White" FontWeight="Bold" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Runtime Architecture" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Text=".NET 10 WPF (Win-x64 Standalone)" Foreground="#38BDF8" FontSize="12.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Version" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Text="v2.4.0 (Enterprise Build)" Foreground="White" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Repository" Foreground="#94A3B8" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Text="stack-crash999/FreeFing" Foreground="#94A3B8" FontSize="12.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
